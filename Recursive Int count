function realSize(arrays) {
	var counter = 0
	intCount (arrays)
	function intCount (arr){
		for (i=0;i<arr.length;i++){
			if (typeof arr[i] === 'number'){
				counter ++
			}
			else if (arr[i].length !==0) {
				intCount (arr[i])
			}
		}
	}
	return counter
}

realSize ([1,2, []])
